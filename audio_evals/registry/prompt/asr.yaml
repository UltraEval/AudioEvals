asr:
  class: audio_evals.prompt.Prompt
  args:
    template:
      - role: user
        contents:
          - type: audio
            value: "{{file}}"
          - type: text
            value: 这个语音说了什么

alei:
  class: audio_evals.prompt.Prompt
  args:
    template:
      - role: user
        contents:
          - type: audio
            value: "/Users/a1/project/UltraEval/datasets/mb-temp-model-reuse-aqa/data/{{audio}}"
          - type: text
            value: 回复语音内容

KeSpeech:
  class: audio_evals.prompt.Prompt
  args:
    template:
      - role: user
        contents:
          - type: audio
            value: "/Users/a1/project/AudioEvals/{{WavPath}}"
          - type: text
            value: "输出语音内容，格式为{\"content\": \"\"}"

multi-libri:
  class: audio_evals.prompt.Prompt
  args:
    template:
      - role: user
        contents:
          - type: audio
            value: "{{WavPath}}"
          - type: text
            value: "output the content with format {\"content\": \"\"}"

convost2_en_zh:
  class: audio_evals.prompt.Prompt
  args:
    template:
      - role: user
        contents:
          - type: audio
            value: "{{audio}}"
          - type: text
            value: "translate english audio to chinese, output the content with format {\"content\": \"\"}"
